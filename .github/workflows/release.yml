name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build .skill artifacts
        run: |
          bash skills/watch/scripts/build-skill.sh
          bash skills/learn-from-video/scripts/build-skill.sh
          test -f dist/watch.skill
          test -f dist/learn-from-video.skill

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/learn-from-video.skill
            dist/watch.skill
          generate_release_notes: true
          draft: false
          prerelease: false
